{"findings": [{"file": "plugins/workbench/skills/vault-sync/references/command.md", "line": 3, "description": "SPEC explicitly states 'plugins/workbench/skills/vault-sync/** is untouched — that is companion issue #893's footprint,' but the diff adds a new line to this exact path ('Before pulling, run the sync boundary squash if the wrap-up skill has not already done so.'), wiring /sync to invoke the squash script. This is scope creep into a path the spec promises is out of scope for this PR, introducing untested/unreviewed behavior (a second, undocumented invocation path for the squash script from /sync) that the spec never asked for."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 115, "description": "classify_pushed_prefix() returns `first_unpushed + 1` instead of `first_unpushed`, an off-by-one that breaks the pushed/unpushed prefix computation for every 2+ commit session. Concrete failure: with 2 session commits that are both already pushed (pushed_flags=[True, True], the exact scenario the diff's own test_all_session_commits_pushed_is_a_no_op exercises), first_unpushed becomes 2 (no False found) and the function returns 3 instead of 2. squash() then sets report.unpushed = len(session) - prefix = 2 - 3 = -1 (matches neither the ==0 nor ==1 no-op branches) and falls through to `squash_base = session[prefix - 1]` = `session[2]`, indexing past the end of a 2-element list — an uncaught IndexError crash instead of the required action:'none'. The same bug also misclassifies the partial-pushed case (e.g. test_pushed_first_commit_is_not_rewritten's [True, False, False] should yield prefix=1 but yields 2), causing that scenario to short-circuit to action:'none' ('only one unpushed commit') instead of squashing, contradicting the SPEC guard 'the newest pushed session commit becomes the squash base and only the commits after it collapse.'"}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 158, "description": "SPEC's guard list requires 'staged-but-uncommitted changes' to report skipped and change nothing, and the diff's own test_staged_uncommitted_changes_skip_squash and the sync_boundary_squash.teeth.json mutant ('staged-but-uncommitted changes no longer skip the squash', which searches for a `git diff --cached --quiet` check) both assume this guard exists — but squash() contains no such check anywhere between the operation-in-progress guard and this line (`head = _git_out(\"rev-parse\", \"HEAD\")`) or anywhere else in the function. Concrete failure: stage a file (`git add staged.md`) without committing it, then run the squash with 2+ unpushed session commits present. With no staged-changes guard, `git reset --soft <squash_base>` leaves the staged content in the index untouched, and the subsequent `git commit --quiet -C <head>` commits whatever is currently staged — silently sweeping the uncommitted staged file into the new squashed wrap-up commit instead of skipping and leaving the repository untouched, violating both the SPEC guard and the fail-open contract."}]}