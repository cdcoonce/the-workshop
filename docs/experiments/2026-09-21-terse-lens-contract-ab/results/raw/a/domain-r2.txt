{"findings": [{"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 207, "description": "pushed_flags = [_is_ancestor(remote_head, sha) for sha in session] has the ancestry-check arguments reversed. _is_ancestor(ancestor, descendant) reports whether `ancestor` is an ancestor of `descendant`; determining whether a session commit `sha` is pushed requires asking whether `sha` is an ancestor of `remote_head` (_is_ancestor(sha, remote_head)), not the reverse. Concrete case: base commit is pushed to origin (remote_head == base); three session commits s1, s2, s3 are built locally on top of base and never pushed. For every sha in [s1, s2, s3], _is_ancestor(remote_head, sha) asks 'is base an ancestor of s1/s2/s3', which is true for all three since they descend from base. pushed_flags therefore comes back [True, True, True] even though none of the three commits were ever pushed -- exactly the scenario in this diff's own test_collapses_three_unpushed_commits_into_one, which expects report['pushed']==0 and report['unpushed']==3."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 115, "description": "classify_pushed_prefix returns `first_unpushed + 1` instead of `first_unpushed`, an off-by-one that overcounts the pushed prefix by one commit in every non-interleaved case. Concrete case: pushed_flags = [True, True] (both session commits already pushed, e.g. test_all_session_commits_pushed_is_a_no_op with a 2-commit session). The loop never encounters a False, so first_unpushed keeps its initial value len(pushed_flags) == 2; the function returns 2 + 1 == 3. Back in squash(), report.unpushed = len(session) - prefix = 2 - 3 == -1, which matches neither the `unpushed == 0` nor `unpushed == 1` early-return guards, so control falls through to `squash_base = session[prefix - 1]` == `session[2]` on a 2-element list (valid indices 0-1), raising an uncaught IndexError instead of the required graceful `none` report -- a crash (non-zero exit from an unhandled exception, not the documented skipped/none/squashed contract) in a case the spec and its own test explicitly require to be a clean no-op."}]}