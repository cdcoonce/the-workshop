{"findings": [{"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 207, "description": "pushed_flags is built with `_is_ancestor(remote_head, sha)` (\"is remote_head an ancestor of sha\") which is the wrong direction for a pushed-ness check; it should be `_is_ancestor(sha, remote_head)` (the correct order is used two lines earlier for `_is_ancestor(base, head)`). Concrete scenario: ordinary single-machine case, base is pushed, 3 new session commits sit on top unpushed, no divergence. remote_head resolves to the pushed base SHA, which is trivially an ancestor of all three descendant commits, so every one of them is misclassified as already pushed regardless of actual push state. Combined with the classify_pushed_prefix bug this yields `squash_base = session[3]` on a 3-element list -> IndexError, crashing the script; even if that bug were fixed alone, an all-True pushed_flags makes report.unpushed == 0, so the script reports \"none\" and squashes nothing in exactly the scenario acceptance test (a) (`test_collapses_three_unpushed_commits_into_one`) is supposed to prove, defeating the feature's purpose."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 115, "description": "classify_pushed_prefix returns `first_unpushed + 1` instead of `first_unpushed`, overcounting the pushed prefix by one. This is falsified by the diff's own parametrized unit test test_classify_pushed_prefix (no git/subprocess involved): flags=[] expects 0 but the code returns 0+1=1; [True, False, False] expects 1 but returns 1+1=2; [True, True] expects 2 but returns 2+1=3 -- 4 of 6 parametrized cases fail outright. Downstream in squash(), the inflated prefix makes `squash_base = session[prefix - 1]` pick a later or out-of-range commit (e.g. session[3] on a 3-element list -> IndexError), or falsely treats a never-pushed commit as pushed, breaking acceptance test (b)'s guarantee: test_pushed_first_commit_is_not_rewritten expects report['pushed']==1 and squash_base==c1, both wrong by one under this code."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 233, "description": "squash() never checks for staged-but-uncommitted changes before rewriting history, although the issue's Guards list (\"staged-but-uncommitted changes ... all report skipped and change nothing\") and the command.md text this same diff adds (\"On any guard -- a merge commit in the session range, staged-but-uncommitted changes, an unreachable remote, an ambiguous pushed/unpushed classification -- it reports skipped\") both claim this guard exists. Concrete scenario: two unpushed session commits plus one file `git add`-ed but not committed. Nothing intercepts this; execution reaches `_git_out(\"reset\", \"--soft\", squash_base)` (line 233) and `commit -C head\", which snapshots whatever is currently staged, so the resulting tree diverges from head^{tree} and the later verification check reports a generic \"squashed commit failed verification; repository restored\" instead of a staged-specific reason. The shipped test test_staged_uncommitted_changes_skip_squash asserts `\"staged\" in report[\"reason\"]`, which is false for that generic message, so this test fails against the actual code."}, {"file": "plugins/workbench/skills/vault-sync/references/command.md", "line": 3, "description": "The PR description states \"`plugins/workbench/skills/vault-sync/**` is untouched -- that is companion issue #893's footprint,\" but this diff edits vault-sync/references/command.md, adding \"Before pulling, run the sync boundary squash if the wrap-up skill has not already done so.\" This is scope SPEC explicitly says was not touched, so it was never described or reviewed as part of this PR. It is also functionally incomplete on its own: unlike the vault-wrap-up command.md addition in this same diff, which gives an explicit invocation (`python3 \"<skill>/scripts/sync_boundary_squash.py\" --base <commit before this session's first commit> --json`) with a defined `<skill>` base-dir resolution, this /sync sentence gives no script path, no way to locate a script that physically lives under a different skill's (vault-wrap-up) directory, and no --base value (the wrap-up flow's \"session base\" concept is never defined anywhere in vault-sync). Following this instruction standalone has no concrete procedure."}]}