{"findings": [{"file": "plugins/workbench/skills/vault-wrap-up/scripts/tests/test_sync_boundary_squash.py", "line": 344, "description": "test_classify_pushed_prefix asserts classify_pushed_prefix(flags) == expected for [([], 0), ([False,False,False], 0), ([True,False,False], 1), ([True,True], 2), ([True,False,True], None), ([False,True], None)]. The function it tests (sync_boundary_squash.py new-file line 238: `return first_unpushed + 1`) has an off-by-one: it should return `first_unpushed` (the docstring literally says 'Length of the leading pushed run', and first_unpushed already holds that count when the loop breaks or exhausts). Tracing the actual code: classify_pushed_prefix([])->1 not 0; ([False,False,False])->1 not 0; ([True,False,False])->2 not 1; ([True,True])->3 not 2. 4 of 6 parametrized cases fail against the implementation shipped in this same diff, so this test cannot pass as claimed by the PR's '22 acceptance tests... all green' / '10/10 mutants killed' verification. Independent proof: teeth.json mutant #10 (sync_boundary_squash.teeth.json) has `find: '...return first_unpushed'` (no `+ 1`) for this exact line — that string does not exist in the shipped source, so that mutant could never even be applied by the mutation tool."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/tests/test_sync_boundary_squash.py", "line": 199, "description": "test_pushed_first_commit_is_not_rewritten (acceptance criterion b: 'a session commit already on the remote is never rewritten') builds session=[c1(pushed), c2, wrapup] and expects report['pushed']==1, report['unpushed']==2, report['squash_base']==c1 (asserted at line 204). But squash() (sync_boundary_squash.py new-file line 207) computes `pushed_flags = [_is_ancestor(remote_head, sha) for sha in session]` — the arguments are reversed: `_is_ancestor(A, B)` checks whether A is an ancestor of B, so this checks 'is remote_head older-than-or-equal-to sha', which is true for every commit downstream of remote_head in a linear chain, not just the ones actually on the remote. With remote_head=c1, `_is_ancestor(c1, c1)`, `_is_ancestor(c1, c2)`, and `_is_ancestor(c1, wrapup)` are all True (c2 and wrapup both descend from c1), so pushed_flags becomes [True, True, True] instead of the intended [True, False, False]. Combined with the off-by-one in classify_pushed_prefix, this yields prefix=4 for a 3-element session list, and `squash_base = session[prefix - 1]` (line ~348) evaluates session[3], raising an IndexError. The subprocess exits non-zero with a traceback, so `_run_squash`'s own `assert result.returncode == 0` (which the call at line 199 triggers) fails immediately — the test crashes before ever reaching the squash_base==c1 assertion it claims to prove."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/tests/test_sync_boundary_squash.py", "line": 362, "description": "test_staged_uncommitted_changes_skip_squash (starting at line 352) stages an uncommitted file and asserts report['action']=='skipped' with 'staged' in report['reason'], per the spec's guard list ('staged-but-uncommitted changes ... report skipped and change nothing'). But squash() in sync_boundary_squash.py (the full function, new-file lines ~100-262) contains no staged/uncommitted-changes check anywhere — there is no `git diff --cached` call in the shipped file. The guard described in command.md and spec.md, and exercised by this test, was never implemented. Concrete failure: with 2 session commits and a staged-but-uncommitted staged.md, squash() runs straight through every existing guard, reaches the broken pushed/unpushed classification (both session commits get marked 'pushed' by the reversed _is_ancestor check described above), and raises an IndexError computing squash_base — the process never reaches action=='skipped' and the assertion at line 362 (patch line 899) fails. Independent confirmation: teeth.json's mutant labeled 'staged-but-uncommitted changes no longer skip the squash' has a `find` block referencing `staged = _run_git(\"diff\", \"--cached\", \"--quiet\")` — that code does not exist anywhere in sync_boundary_squash.py, so this mutant could never have been applied, contradicting the PR's '10/10 mutants killed' claim."}]}