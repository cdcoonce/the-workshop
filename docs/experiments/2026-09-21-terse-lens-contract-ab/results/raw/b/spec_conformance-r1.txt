{"findings": [{"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 207, "description": "pushed_flags is computed as `_is_ancestor(remote_head, sha)` (\"is remote_head an ancestor of sha\"), which is backwards from the needed \"is sha already reachable from remote_head\" (i.e. `_is_ancestor(sha, remote_head)`). Every session commit descends from remote_head by construction, so this marks commits that were never pushed as pushed. Concrete failure against the PR's own acceptance scenario (b) (test_pushed_first_commit_is_not_rewritten): c1 is pushed, c2/c3 are not, remote_head == c1; `_is_ancestor(c1, c2)` and `_is_ancestor(c1, c3)` both evaluate True in addition to c1 itself, giving pushed_flags = [True, True, True] instead of [True, False, False]. Combined with the off-by-one at line 115, classify_pushed_prefix returns 4 for a 3-commit session, so `session[prefix - 1]` at line 225 indexes past the end of a 3-element list and the script crashes with an unhandled IndexError instead of squashing — the opposite of the documented fail-open contract."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 115, "description": "classify_pushed_prefix returns `first_unpushed + 1` instead of `first_unpushed`, overcounting the pushed-prefix length by one in every non-interleaved case. This contradicts its own shipped unit test table (test_sync_boundary_squash.py::test_classify_pushed_prefix): classify_pushed_prefix([]) returns 1 where the test asserts 0, and classify_pushed_prefix([True, True]) returns 3 where the test asserts 2 (one past the end of a 2-element list). Downstream in squash(), `session[prefix - 1]` (line 225) then indexes past the end of `session` whenever every commit in range is pushed, and `report.unpushed = len(session) - prefix` (line 214) goes negative, so neither the 'all pushed' guard (line 216) nor the 'single unpushed commit' guard (line 220) can ever fire."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 168, "description": "SPEC requires 'staged-but-uncommitted changes' to be one of the conditions that make the boundary report `skipped` and leave the repository untouched. The shipped test_sync_boundary_squash.py::test_staged_uncommitted_changes_skip_squash asserts exactly that (action 'skipped', 'staged' in reason), and the teeth fixture's mutant 'staged-but-uncommitted changes no longer skip the squash' references a `git diff --cached --quiet` check block as existing code to mutate — but squash() goes directly from the merge-commit check to gathering rev_list here without ever inspecting the index; no such check exists anywhere in the 295-line file. Running the boundary with a staged-but-uncommitted file and 2+ otherwise-ordinary unpushed commits proceeds straight into the reset/commit sequence instead of skipping, so the referenced test would fail."}, {"file": "plugins/workbench/skills/vault-sync/references/command.md", "line": 3, "description": "SPEC explicitly states 'plugins/workbench/skills/vault-sync/** is untouched — that is companion issue #893's footprint,' but this diff adds a line to vault-sync's own command reference ('Before pulling, run the sync boundary squash if the wrap-up skill has not already done so.'). This is an out-of-scope change to a path SPEC reserves for a different issue, and it introduces an undefined detection mechanism — nothing else in the diff gives a bare `/sync` invocation any way to determine 'if the wrap-up skill has not already done so.'"}]}