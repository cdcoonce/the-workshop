{"findings": [{"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 115, "description": "classify_pushed_prefix returns `first_unpushed + 1` instead of `first_unpushed`, an off-by-one that overcounts the pushed prefix by one commit. Concrete failure: acceptance scenario (b) — first session commit already pushed, two more commits after it (pushed_flags=[True, False, False], as in the PR's own test_pushed_first_commit_is_not_rewritten) — the correct prefix is 1 (only the first commit is pushed), but the function returns 2. In squash(), report.unpushed = len(session) - prefix = 3 - 2 = 1, which hits the `unpushed == 1` branch and reports action='none' (nothing to collapse) instead of squashing the two actually-unpushed commits onto the pushed commit as squash base — contradicting both the spec's guard ('the newest pushed session commit becomes the squash base and only the commits after it collapse') and the PR's own acceptance test, which asserts action=='squashed', pushed==1, unpushed==2. The function's own parametrized unit test table in test_sync_boundary_squash.py (test_classify_pushed_prefix) also contradicts this return value directly: inputs [], [False,False,False], [True,False,False], and [True,True] expect 0, 0, 1, 2 respectively, but the shipped code returns 1, 1, 2, 3."}, {"file": "plugins/workbench/skills/vault-wrap-up/scripts/sync_boundary_squash.py", "line": 207, "description": "pushed_flags is computed as `_is_ancestor(remote_head, sha)` — i.e. 'is remote_head an ancestor of sha' — which is backwards for determining whether a session commit sha has been pushed. Pushed-ness requires the opposite ancestry test, `_is_ancestor(sha, remote_head)` (is sha reachable from remote_head, i.e. already on the remote). The existing convention in this file (`_is_ancestor(base, head)` at the earlier guard, checking base precedes head) confirms the intended argument order is (potential-ancestor, potential-descendant), so this call has the arguments reversed. Concrete failure: acceptance scenario (a) — three unpushed commits built directly on the pushed base with no peer activity (test_collapses_three_unpushed_commits_into_one) — remote_head equals base, and base is trivially an ancestor of every commit built on top of it, so `_is_ancestor(remote_head, sha)` returns True for all three session commits, misclassifying every unpushed commit as pushed. classify_pushed_prefix then treats the whole session as a pushed prefix (prefix=3, or with the sibling off-by-one bug, prefix=4), driving report.unpushed to 0 or negative; with both bugs present, `squash_base = session[prefix - 1]` indexes with prefix=4 into a 3-element list, raising an unhandled IndexError (only GitError is caught in main()), so the script crashes on the single most basic scenario the feature exists for instead of squashing or reporting a clean skip/none."}]}