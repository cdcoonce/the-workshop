# Router policy and AFK guard

1. Make the universal router resolve repository policy and integration targets
   before selecting workflow skills.
2. Add an explicit AFK mode that follows local unattended-execution constraints
   and escalates genuine capability gaps through the repository mechanism.
3. Add regression coverage, regenerate shipped outputs, and validate the full
   gate before integrating through `dev`.
